# Neon Postgres connection string
# DATABASE_URL="postgresql://user:password@host/dbname?sslmode=require"

# PORT=5000

# # Long random string — generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# JWT_SECRET=replace_with_long_random_string

# # Admins live in the database now (see Admin model in schema.prisma).
# # Create the first one with: npm run create-admin -- you@example.com yourpassword "Your Name"
